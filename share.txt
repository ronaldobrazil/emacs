ゾーン収容マップから装置構成、中央と保守制御共有

保守制御イベント、状態引き継ぎ方式

コールドスタンバイなら、ファイルに書くしか無い共有ディスクにして、参照。
ウォームスタンバイなら、DRBD


監視の設定ファイル例とオニオンコア、stateモナド　規模テスト削減

既設の装置構成情報.xml に、保守制御から見てのホップ数を追加、保守制御からみて手前のポップノードを規定
ホップ使ってその先不定にする障害項目や通信異常を定義
エリア、ゾーンは、これまで通りだが、基地局ネットワークの構成がエリア単位
物理回線断とVLAN断、片系断がある
装置、部位1, 部位2 で階層化
両系MNでMJは装置、部位、レベルで。
基地局TRX冗長と縮退判断は？
アラームマスクリスト、インスタンスとクラス両方対応。
基地局は、保守制御からみたネットワーク断と中央制御が通信出来ているかがあるが、前者はマイナー、後者はメジャー障害。
イベントログの保存条件は、ロジックにする？
基地局ネットワークのサブネットワーク箇所は、ホップで対応？
CTカードで特別な切り替え時見せ方があったような。
設定とアラームマスクと局操、外部入力と共有メモリ出力はモナド。
それ以外は数学的関数

非監視設定は、ノードとネットワーク単位で
非監視と、IP集中監視に出力は別
エリア間は、サマリと通知のみ

既設は、装置、カード、回路毎にどう取り扱うかのロジックをソースで作り込んでいた。
電文もそれぞれで解析コードが必要。


既設保守制御、電文詰め替え規模無駄。一回にする意味でも正規化

イベントと現在の状態とルール、

public volatile bool isContinue;

int main(int argc, char* argv[])
{
        isContinue = true;

        try
        {
                CurrentStatus st;
                RuleSet rule;
                Target target:
                
                bool initSelfResult = initializeSelf(target,rule);
                bool initStateResult = initializeStatus(target,st);

                while(isContinue)
                {
                        ev = receiveEvent(target);
                        st = effectEventProcess(ev, st, rule);
                }
                finalize();
        }
        catch(Exception ...)
        {
        }
        return 1;
}


public CurrentStatus effectEventProcess(ManagedEvent &ev, CurrentStatus &st, RuleSet &rule)
{
        

}
